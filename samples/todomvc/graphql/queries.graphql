query GetAll {
  todos {
    id
    title
    completed
  }
}

mutation DeleteTodo($id: ID) {
  deleteTodo(id: $id) {
    id
  }
}

mutation CreateTodo($title: String) {
  createTodo(todo: { title: $title }) {
    id
    title
    completed
  }
}
